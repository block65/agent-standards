prefer optional fields instead of defaults, keep control at the app layer.

use tense-less names for all fields, example create_time, expire_time

most if not all tables should have a create_time and an update_time

use is_ for boolean, with no default. 